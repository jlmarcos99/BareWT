{
  "name": "barewt",
  "version": "0.0.0",
  "description": "CLI for git worktrees using the bare clone pattern",
  "keywords": [
    "git",
    "worktree",
    "bare",
    "cli"
  ],
  "license": "MIT",
  "author": "jmarcos <joseluespp@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CAMBIAME/barewt.git"
  },
  "type": "module",
  "bin": {
    "bwt": "./dist/main.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run typecheck && npm run lint && npm run test && npm run build",
    "changeset": "changeset",
    "release": "npm run build && changeset publish"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "execa": "^9.6.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@changesets/cli": "^2.31.0",
    "@types/node": "^26.1.1",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
